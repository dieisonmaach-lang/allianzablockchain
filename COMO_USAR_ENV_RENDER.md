# 📋 COMO USAR .env NO RENDER

## 🎯 MÉTODO: "Add from .env"

O Render tem uma opção **"Add from .env"** que importa automaticamente todas as variáveis do arquivo `.env`.

---

## ✅ PASSO A PASSO

### 1. Use o arquivo `.env.limpo` que criei

O arquivo `.env.limpo` está **sem duplicatas** e pronto para usar.

### 2. No Render Dashboard:

1. **Acesse:** Settings → Environment
2. **Clique em:** "Add from .env"
3. **Cole o conteúdo** do arquivo `.env.limpo`
4. **O Render detectará automaticamente** todas as variáveis
5. **Clique em:** "Save and deploy"

---

## 📝 CONTEÚDO DO .env.limpo

O arquivo `.env.limpo` contém **TODAS as variáveis necessárias, SEM duplicatas:**

✅ APIs (INFURA, BLOCKCYPHER)
✅ Chaves privadas (POLYGON, ETH, BASE, SOLANA, BITCOIN)
✅ RPCs (ETH, POLYGON, BASE, SOLANA)
✅ Configurações (BLOCKCHAIN_MODE, ENCRYPTION_KEY)
✅ Contratos (BRIDGE_CONTRACT_ETH, BRIDGE_CONTRACT_POLY)
✅ Endereços (POLYGON_MASTER_WALLET, BASE_ADDRESS, etc.)

**Total: 25 variáveis únicas** (sem duplicatas)

---

## 🔧 SE O RENDER NÃO RECONHECER AUTOMATICAMENTE

Se a opção "Add from .env" não funcionar:

1. **Copie o conteúdo** do arquivo `.env.limpo`
2. **Cole no campo** "Add from .env"
3. **Ou adicione manualmente** cada variável (mas o .env é mais rápido)

---

## ✅ APÓS IMPORTAR

1. **Verifique** se todas as variáveis foram importadas
2. **Remova** qualquer duplicata que aparecer
3. **Clique em:** "Save and deploy"
4. **Aguarde** o deploy (5-10 minutos)
5. **Teste transferências reais!** 🚀

---

## 📋 RESUMO

- ✅ Arquivo `.env.limpo` criado (sem duplicatas)
- ✅ 25 variáveis únicas
- ✅ Pronto para colar no Render
- ✅ Use "Add from .env" no Render

**Cole o conteúdo do `.env.limpo` no Render e pronto!** ✅

